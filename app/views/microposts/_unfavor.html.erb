<%= form_with(model: current_user.favorites.find_by(micropost_id: @micropost.id),
            html: { method: :delete }, remote: true) do |f| %>

  <%= f.button type: "submit", name: nil,　class: "form-control" do %>
    <i class="fas fa-heart unfavor-heart"></i>
  <% end %>
  <%#= f.submit "Unfavor", class: "btn" %>
<% end %>
