<%= form_with(model: current_user.favorites.build, remote: true) do |f| %>
  <div><%= hidden_field_tag :micropost_id, @micropost.id %></div>

  <%= f.button type: "submit", name: nil,　class: "form-control" do %>
    <i class="fas fa-heart favor-heart"></i>
  <% end %>

  <%#= f.submit "Favor", class: "btn btn-primary" %>
<% end %>
